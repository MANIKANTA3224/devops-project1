[Unit]
Description=Python Flask App
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/opt/app
ExecStart=/opt/app/venv/bin/python -m flask run --host=127.0.0.1 --port=5000
Environment="FLASK_APP=app.py"
Restart=always

[Install]
WantedBy=multi-user.target
